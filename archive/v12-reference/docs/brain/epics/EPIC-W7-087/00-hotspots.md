# EPIC-W7-087 · Phase 0 — Hotspot Analysis

**Symbol:** `AuditFleet_CheckWorkingStop`  
**Source:** `src/V12_002.REAPER.Audit.cs` · line 517  
**Wave:** 7 | **CYC:** 0 (confirmed)

---

## 1. Symbol Summary

`AuditFleet_CheckWorkingStop(Account acct)` is a private, branchless predicate that snapshots `acct.Orders` and returns `true` when any order matches all four conditions: correct instrument, working/accepted state, stop-type (StopMarket or StopLimit), and a sell-side action (Sell or BuyToCover). CYC is **0** — no branching inside the method body beyond the single LINQ predicate, which counts as a sequence node, not a decision node.

---

## 2. Call Graph (immediate context)

```
AuditSingleFleetAccount (CYC: ~8)
  └── AuditFleet_HandleNakedPosition            ← calls AuditFleet_CheckWorkingStop
        ├── AuditFleet_CheckWorkingStop          ← SUBJECT (CYC: 0)
        └── DetectNakedPosition  (on miss)
              ├── CheckPendingStopReplace
              ├── EvaluateNakedPositionGrace
              └── EnqueueEmergencyStop
                    └── TriggerCustomEvent → ProcessReaperNakedStopQueue
```

**Dual-path note:** `AuditMaster_HandleNakedPosition` (line 624) contains an **inlined copy** of the same four-predicate stop-check logic on `Account.Orders` — it does NOT call `AuditFleet_CheckWorkingStop`. This is the primary structural hotspot.

---

## 3. Hotspots Identified

| # | Location | Observation |
|---|----------|-------------|
| H1 | `AuditFleet_CheckWorkingStop` → `AuditMaster_HandleNakedPosition` | Logic duplication: the identical 4-condition stop-order predicate is copy-pasted at line 631–636. Any future change to stop-detection conditions must be applied in two places. |
| H2 | `AuditMaster_HandleNakedPosition` (line 630) | Master path snapshots `Account.Orders.ToArray()` inline and evaluates the predicate without going through the fleet helper. The fleet path uses `AuditFleet_CheckWorkingStop`; master bypasses it entirely. |
| H3 | `AuditFleet_CheckWorkingStop` | The predicate operates on `acct.Orders.ToArray()` (heap allocation + full copy on every audit tick). At fleet scale this is an O(n) allocation per account per audit cycle. Low severity individually but multiplies across all fleet accounts. |
| H4 | `AuditFleet_HandleNakedPosition` | `ClearNakedPositionGrace` is called on the happy path (stop found) but there is no equivalent logging — a stop transitioning from missing→present is entirely silent. |

---

## 4. Blast Radius

| Caller | File | Risk on change |
|--------|------|----------------|
| `AuditFleet_HandleNakedPosition` | `V12_002.REAPER.Audit.cs:343` | Direct — owns the result; drives `DetectNakedPosition` gating |
| `AuditMaster_HandleNakedPosition` | `V12_002.REAPER.Audit.cs:631` | Indirect — inlined copy, not a caller; must be kept in sync manually |
| `ClearNakedPositionGrace` / `_nakedPositionFirstSeen` | `V12_002.REAPER.cs:92` | State side-effect dependent on result of stop-check |
| `ProcessReaperNakedStopQueue` | `V12_002.REAPER.NakedStop.cs:21` | Downstream consumer of any missed detection |
| `V12_002.UI.Compliance.cs:533` | `_nakedPositionFirstSeen` write | Cross-cutting write to same grace-tracking dictionary |
| `V12_002.Orders.Callbacks.AccountOrders.cs:88,124` | `_nakedPositionFirstSeen` write | Order-fill callbacks clear grace; race-free only because of per-key ConcurrentDictionary semantics |

**Blast radius scope:** Confined to the naked-position detection pipeline. No FSM state, no flatten queue, and no repair queue are touched by this method directly.

---

## 5. Structural Risk Assessment

| Risk | Severity | Notes |
|------|----------|-------|
| Logic drift between fleet and master paths | **Medium** | H1/H2 — the inlined master copy is a maintenance hazard, not a current defect |
| Per-tick heap allocation (`ToArray`) | **Low** | Acceptable in the current audit cadence (30 s heartbeat); not on a hot path |
| Silent grace clear (H4) | **Low** | Observability gap only; no correctness impact |
| Missing stop triggers incorrect naked-detection pipeline | **High** (if predicate wrong) | But predicate is correct and well-tested across builds 935–1108 |

---

## 6. Recommended Phase 1 Actions

1. **Extract** the four-condition stop predicate into a shared private helper (e.g., `HasWorkingStopOrder(Account acct)`) so both fleet and master paths share one definition.
2. **Eliminate** the inlined copy in `AuditMaster_HandleNakedPosition` by delegating to the shared helper.
3. (Optional) Add a `shouldLog` trace line in `AuditFleet_HandleNakedPosition` for the `hasWorkingStop == true` branch to close the observability gap (H4).

---

*Generated by Bob · Wave 7 Phase 0*
