# PR-21 Lane L2-REPAIR-V2 -- Confirmed Edit Plan

**Generated by**: Phase 2 Architecture Planner (v12-phase2-architecture mode)
**Status**: APPLIED -- all 9 operations executed successfully
**ASCII clean**: no em-dash bytes (0xe2 0x80 0x94) in any replacement text

---

## Source File Verification

All 3 files were read from the worktree at `/tmp/wt-pr21` after restoring from the
branch `wave7/pr2-s3-ui-ipc` (files had been externally deleted; restored via
`git checkout -- <files>`).

| File | Search text verified |
|------|----------------------|
| `src/V12_002.UI.IPC.Commands.Fleet.cs` | FIX 1 (line 512), FIX 6 (region ~511), FIX 7 (lines 787-788) -- all confirmed |
| `src/V12_002.UI.IPC.Commands.Mode.cs` | FIX 2 (line 148-149), FIX 3 (lines 203-205), FIX 4 (lines 252-254), FIX 5 (lines 359-360) -- all confirmed |
| `src/V12_002.UI.Compliance.cs` | FIX 8 (lines 846-852), FIX 9 (lines 952-954) -- all confirmed |

---

## Ambiguities Found and Resolved

### Ambiguity 1: `minContracts` scope in FIX 1

The brief's replacement text uses `minContracts` inside `TryExecuteRmaEntry`.
Investigation confirmed `minContracts` is a **class-level field** (partial class),
used in `CalculateIpcEntryQty` (Fleet.cs lines 469, 475) and `TryHandleRisk_SetTargets`
(Mode.cs line 416) without a local `int` declaration -- confirming it is a field in
scope for `TryExecuteRmaEntry`. Replacement is valid.

### Ambiguity 2: FIX 7 trailing blank line

The source has a blank line (line 789) after `return false;` and before `if (parts.Length...`.
The brief's SEARCH text ends at `return false;\n` without including that blank line.
The replacement ends at `}\n`. After apply, the existing blank line remains as a trailing
separator, satisfying SA1513 (blank line after closing brace). No issue.

---

## Final Confirmed Edit Plan (9 distinct operations)

### Operation order for `Fleet.cs`: FIX 1 first, then FIX 6, then FIX 7

---

### FILE 1: `src/V12_002.UI.IPC.Commands.Fleet.cs`

#### FIX 1 -- Safety guard on zero stopDist (applied first)

```
SEARCH (line 512):
            int contracts = CalculatePositionSize(stopDist);

REPLACE:
            int contracts = stopDist > 0 ? CalculatePositionSize(stopDist) : Math.Max(1, minContracts);
```

**Rationale**: Greptile P1 safety bug -- MinimumStop==0 (unconfigured) still yields
stopDist==0 after the fallback block, causing CalculatePositionSize(0) to run. Guard
pattern matches the existing `CalculateIpcEntryQty` safe pattern.

---

#### FIX 6 -- SA1513 blank line after closing brace (applied after FIX 1)

```
SEARCH (after FIX 1 is in place):
            }
            int contracts = stopDist > 0 ? CalculatePositionSize(stopDist) : Math.Max(1, minContracts);

REPLACE:
            }

            int contracts = stopDist > 0 ? CalculatePositionSize(stopDist) : Math.Max(1, minContracts);
```

---

#### FIX 7 -- SA1503 braces required on single-line if

```
SEARCH (lines 787-788):
            if (action != "SET_SHADOW")
                return false;

REPLACE:
            if (action != "SET_SHADOW")
            {
                return false;
            }
```

Note: existing blank line at line 789 (between `return false;` and the next `if`) is
preserved -- it is outside the search block and satisfies SA1513 after the new `}`.

---

### FILE 2: `src/V12_002.UI.IPC.Commands.Mode.cs`

#### FIX 2 -- SA1515 blank line before single-line comment

```
SEARCH (lines 148-149):
            }
            // ATOMIC mode transition: clear all flags, then set the new mode

REPLACE:
            }

            // ATOMIC mode transition: clear all flags, then set the new mode
```

---

#### FIX 3 -- SA1513 blank line after closing brace

```
SEARCH (lines 203-205):
                );
            }
            BumpUiConfigRevision();

REPLACE:
                );
            }

            BumpUiConfigRevision();
```

---

#### FIX 4 -- SA1503 braces required on single-line if

```
SEARCH (lines 252-254):
            if (TryHandleRisk_SetManualPrice(action, parts))
                return true;
            return false;

REPLACE:
            if (TryHandleRisk_SetManualPrice(action, parts))
            {
                return true;
            }

            return false;
```

---

#### FIX 5 -- SA1513 blank line after closing brace

```
SEARCH (lines 358-360):
                return customTicks * tickSize;
            }
            if (action == "BE" || action == "BE_PLUS_2")

REPLACE:
                return customTicks * tickSize;
            }

            if (action == "BE" || action == "BE_PLUS_2")
```

---

### FILE 3: `src/V12_002.UI.Compliance.cs`

#### FIX 8 -- SA1111+SA1009 closing paren on last-parameter line

```
SEARCH (lines 846-852):
                Print(
                    string.Format(
                        "[ProcessQueuedExecution] Fleet {0} is Flat -- expectedPositions cleared for {1}",
                        fleetAcct.Name,
                        Instrument.FullName
                    )
                );

REPLACE:
                Print(
                    string.Format(
                        "[ProcessQueuedExecution] Fleet {0} is Flat -- expectedPositions cleared for {1}",
                        fleetAcct.Name,
                        Instrument.FullName));
```

---

#### FIX 9 -- SA1111+SA1009 closing paren on last-parameter line

```
SEARCH (lines 952-954):
            var brokerPos = acct.Positions.FirstOrDefault(p =>
                p.Instrument != null && p.Instrument.FullName == Instrument.FullName
            );

REPLACE:
            var brokerPos = acct.Positions.FirstOrDefault(p =>
                p.Instrument != null && p.Instrument.FullName == Instrument.FullName);
```

---

## Execution Summary

All operations were applied via `python3` string replacement (exact match, assert count==1).

| Fix | File | Applied | Occurrences matched |
|-----|------|---------|---------------------|
| FIX 1 | Fleet.cs | Yes | 1 |
| FIX 6 | Fleet.cs | Yes | 1 |
| FIX 7 | Fleet.cs | Yes | 1 |
| FIX 2 | Mode.cs | Yes | 1 |
| FIX 3 | Mode.cs | Yes | 1 |
| FIX 4 | Mode.cs | Yes | 1 |
| FIX 5 | Mode.cs | Yes | 1 |
| FIX 8 | Compliance.cs | Yes | 1 |
| FIX 9 | Compliance.cs | Yes | 1 |

**No em-dash bytes in any replacement text -- ASCII clean.**
