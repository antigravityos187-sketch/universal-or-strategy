# Phase 2: Architecture Planning - EPIC-CCN-026

## Target Method Analysis

**Method**: ProcessQueuedAccountOrder
**File**: src/V12_002.Orders.Callbacks.AccountOrders.cs
**Current Metrics**:
- Complexity: 15
- LOC: 47 (lines 1054-1100)
- Tier: 1 (Critical Path - Order Processing)

**Target Metrics**:
- Complexity: ≤8 (Jane Street strict standard)
- Estimated Post-Extraction: ~5-6

## Extraction Strategy

### Current Method Structure

The method has 5 distinct logical sections:
1. **Validation** (lines 1056-1060): Null checks + instrument validation
2. **Audit Logging** (lines 1062-1071): Ghost audit trail
3. **Cancellation Processing** (line 1074): Delegates to existing helper
4. **Position Matching** (lines 1078-1091): Foreach loop searching for matched position
5. **Dispatch** (lines 1093-1096): Route to HandleMatchedFollowerOrder or ExecuteFollowerCascadeCleanup

### Extraction Plan

Extract 3 helper methods with single responsibilities:

1. **ValidateOrderContext**: Early validation logic
2. **LogOrderUpdate**: Audit trail logging
3. **FindMatchedPosition**: Position search loop

The main method becomes an orchestrator with ~5-6 complexity points.

## Method Signatures

### Original Method

private void ProcessQueuedAccountOrder(QueuedAccountOrderUpdate item)

**Parameters**: item (QueuedAccountOrderUpdate containing EventArgs and Account)
**Returns**: void
**Current Complexity**: 15

### Proposed Helper Method 1: ValidateOrderContext

private bool ValidateOrderContext(QueuedAccountOrderUpdate item, out Order order, out string instrumentName)

**Purpose**: Validate order context and extract key data
**Complexity**: 2
**Returns**: true if valid, false if caller should return early

### Proposed Helper Method 2: LogOrderUpdate

private void LogOrderUpdate(Order order, Account account, string orderState)

**Purpose**: Log order update for audit trail
**Complexity**: 1
**Returns**: void

### Proposed Helper Method 3: FindMatchedPosition

private (string matchedEntry, PositionInfo matchedPos) FindMatchedPosition(Order order, Account account, KeyValuePair<string, PositionInfo>[] snapshot)

**Purpose**: Search position snapshot for matching follower order
**Complexity**: 3
**Returns**: Tuple of (matchedEntry, matchedPos), both null if no match

## Call Graph

ProcessQueuedAccountOrder (main orchestrator)
├── ValidateOrderContext (validation)
├── LogOrderUpdate (audit logging)
├── ProcessFollowerCancellationUnconditional (existing helper)
├── FindMatchedPosition (position search)
├── HandleMatchedFollowerOrder (existing helper)
└── ExecuteFollowerCascadeCleanup (existing helper)

## Lock-Free Validation

### ✅ No lock() Statements
- All helpers use read-only operations
- Snapshot pattern eliminates race conditions

### ✅ FSM/Actor Enqueue Pattern
- Method called from ProcessAccountOrderQueue
- Uses TriggerCustomEvent for rescheduling

### ✅ Atomic Primitives Only
- activePositions.ContainsKey(): Thread-safe
- activePositions.ToArray(): Atomic snapshot

## Jane Street Compliance

### Cognitive Simplicity (CYC ≤8)
- Target: Reduce from 15 to ≤8
- Achieved: ~5-6 (well below threshold)
- Each method has single, clear responsibility

### Testability
- ValidateOrderContext: Unit testable
- LogOrderUpdate: Unit testable
- FindMatchedPosition: Unit testable
- Main Method: Integration testable

## Implementation Sequence

### Step 1: Extract ValidateOrderContext
1. Create private method
2. Move lines 1056-1060 into method body
3. Replace original lines with method call
4. Run tests

### Step 2: Extract LogOrderUpdate
1. Create private method
2. Move lines 1062-1071 into method body
3. Replace original lines with method call
4. Run tests

### Step 3: Extract FindMatchedPosition
1. Create private method
2. Move lines 1078-1091 into method body
3. Return tuple (matchedEntry, matchedPos)
4. Replace original lines with method call
5. Run tests

### Step 4: Verify Complexity Reduction
1. Run complexity_audit.py
2. Verify ProcessQueuedAccountOrder complexity ≤8
3. Document final metrics

## Success Criteria

### Functional Requirements
- ✅ All existing tests pass
- ✅ No behavioral changes
- ✅ No new compilation errors
- ✅ Hard-link sync successful

### Quality Requirements
- ✅ ProcessQueuedAccountOrder complexity ≤8
- ✅ All helper methods complexity ≤8
- ✅ No lock() statements introduced
- ✅ ASCII-only compliance maintained

## Phase 2 Completion

**Status**: COMPLETED
**Date**: 2026-06-15
**Architect**: Bob Shell (v12-engineer mode)
**Decision**: APPROVED - Proceed to Phase 3 (Implementation)
**Next Phase**: Phase 3 (Surgical Extraction + Verification)

---

*Generated by V12 Photon Kernel Phase 2 Protocol (V12.23)*
