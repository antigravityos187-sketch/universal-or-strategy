# PTT-COPIER-B19-L2 — Tickets (Cycle 4)

> Plan status: REVIEW_PASS Cycle 5
> Generated by: ptt-architect (Phase 3 Cycle 4 — final re-issue)
> Key change from Cycle 3: GetAsk() and GetBid() are NO-ARG (use `_instrument` field, no parameter).

---

## T1 — DW-B19-LIMIT-PRICE-01: Ask/Bid anchor fix for Trim/Flatten limit overloads

### Spec requirement IDs satisfied
- DW-B19-LIMIT-PRICE-01

### Files touched
1. `c:\WSGTA\universal-or-strategy\src\PropTraderTools\CopyEngine.cs`
2. `c:\WSGTA\universal-or-strategy\src\PropTraderTools\TradeCopierPanel.cs`
3. `c:\WSGTA\universal-or-strategy\src\PropTraderTools\CopyEngineTests.cs`

---

### CopyEngine.cs — add `ComputeLimitPx`

Add this internal static helper. CYC = 1. Pure arithmetic — no branches.

```csharp
// B19 T1 -- ComputeLimitPx: pure-arithmetic price anchor helper. CYC=1.
internal static double ComputeLimitPx(bool isLong, double ask, double bid, int exitBuffer, double tickSize)
    => isLong ? ask + exitBuffer * tickSize : bid - exitBuffer * tickSize;
```

---

### CopyEngine.cs — replace Trim overload (3-arg → 4-arg)

Old signature (REMOVE):
```
internal void Trim(Instrument instrument, int exitBuffer, double refPrice)
```

New signature (ADD):
```csharp
// B19 T1 -- Trim 4-arg limit overload. CYC=6.
internal void Trim(Instrument instrument, int exitBuffer, double ask, double bid)
{
    if (ask <= 0 || bid <= 0 || exitBuffer == 0) { Trim(instrument); return; }
    double tickSize = instrument.MasterInstrument.TickSize;
    foreach (var acc in AllAccounts(instrument))
    {
        var pos = FindPosition(acc, instrument);
        if (pos == null || pos.Quantity == 0) { StatusUpdate?.Invoke(acc.Name + ": flat skip"); continue; }
        int trimQty = (int)Math.Ceiling(pos.Quantity / 2.0);
        bool isLong = pos.MarketPosition == MarketPosition.Long;
        var action  = isLong ? OrderAction.Sell : OrderAction.BuyToCover;
        double limitPx = ComputeLimitPx(isLong, ask, bid, exitBuffer, tickSize);
        try {
            acc.CreateOrder(instrument, action, OrderType.Limit, OrderEntry.Manual, TimeInForce.Day,
                trimQty, limitPx, 0, null, "PTT-TrimLimit", DateTime.MaxValue,
                (NinjaTrader.Cbi.CustomOrder)null);
            StatusUpdate?.Invoke(acc.Name + ": trim-limit " + trimQty + " @ " + limitPx);
        } catch (Exception ex) { StatusUpdate?.Invoke("PTT-TrimLimit error: " + ex.Message); }
    }
}
```

---

### CopyEngine.cs — replace Flatten overload (3-arg → 4-arg)

Old signature (REMOVE):
```
internal void Flatten(Instrument instrument, int exitBuffer, double refPrice)
```

New signature (ADD):
```csharp
// B19 T1 -- Flatten 4-arg limit overload. CYC=6.
internal void Flatten(Instrument instrument, int exitBuffer, double ask, double bid)
{
    if (ask <= 0 || bid <= 0 || exitBuffer == 0) { Flatten(instrument); return; }
    double tickSize = instrument.MasterInstrument.TickSize;
    foreach (var acc in AllAccounts(instrument))
    {
        var pos = FindPosition(acc, instrument);
        if (pos == null || pos.Quantity == 0) { StatusUpdate?.Invoke(acc.Name + ": flat skip"); continue; }
        bool isLong = pos.MarketPosition == MarketPosition.Long;
        var action  = isLong ? OrderAction.Sell : OrderAction.BuyToCover;
        double limitPx = ComputeLimitPx(isLong, ask, bid, exitBuffer, tickSize);
        try {
            acc.CreateOrder(instrument, action, OrderType.Limit, OrderEntry.Manual, TimeInForce.Day,
                pos.Quantity, limitPx, 0, null, "PTT-FlattenLimit", DateTime.MaxValue,
                (NinjaTrader.Cbi.CustomOrder)null);
            StatusUpdate?.Invoke(acc.Name + ": flatten-limit " + pos.Quantity + " @ " + limitPx);
        } catch (Exception ex) { StatusUpdate?.Invoke("PTT-FlattenLimit error: " + ex.Message); }
    }
}
```

---

### TradeCopierPanel.cs — REMOVE `GetRefPrice()`

Delete the entire `GetRefPrice()` method (lines ~878-886). It is replaced by `GetAsk()` and `GetBid()`.

---

### TradeCopierPanel.cs — ADD `GetAsk()` NO-ARG

```csharp
// B19 T1 -- GetAsk: CYC=4. Uses _instrument field (no parameter).
private double GetAsk()
{
    if (_instrument == null) return 0.0;
    var md = _instrument.MarketData;
    if (md == null)   return 0.0;
    var ask = md.Ask;
    if (ask == null)  return 0.0;
    return ask.Price;
}
```

---

### TradeCopierPanel.cs — ADD `GetBid()` NO-ARG

```csharp
// B19 T1 -- GetBid: CYC=4. Uses _instrument field (no parameter).
private double GetBid()
{
    if (_instrument == null) return 0.0;
    var md = _instrument.MarketData;
    if (md == null)   return 0.0;
    var bid = md.Bid;
    if (bid == null)  return 0.0;
    return bid.Price;
}
```

---

### TradeCopierPanel.cs — UPDATE `OnTrimClick` (CYC=4)

```csharp
private void OnTrimClick(object sender, RoutedEventArgs e)
{
    if (_instrument == null) return;
    double ask = GetAsk();
    double bid = GetBid();
    if (ask <= 0 || bid <= 0 || _trimBuffer == 0)
        _engine.Trim(_instrument);
    else
        _engine.Trim(_instrument, _trimBuffer, ask, bid);
}
```

---

### TradeCopierPanel.cs — UPDATE `OnFlattenClick` (CYC=4)

```csharp
private void OnFlattenClick(object sender, RoutedEventArgs e)
{
    if (_instrument == null) return;
    double ask = GetAsk();
    double bid = GetBid();
    if (ask <= 0 || bid <= 0 || _flattenBuffer == 0)
        _engine.Flatten(_instrument);
    else
        _engine.Flatten(_instrument, _flattenBuffer, ask, bid);
}
```

---

### TradeCopierPanel.cs — UPDATE `DispatchShortcut` Key.T / Key.F

```csharp
case Key.T: _engine.Trim(_instrument, _trimBuffer, GetAsk(), GetBid());       break;
case Key.F: _engine.Flatten(_instrument, _flattenBuffer, GetAsk(), GetBid()); break;
```

---

### CopyEngineTests.cs — UPDATE 5 existing tests

Update each test to pass 4 arguments (Instrument, int, double ask, double bid) instead of 3.
Use the EXACT existing method names listed below.

| # | Existing test method name | Old call | New call |
|---|--------------------------|----------|----------|
| 1 | `Flatten_LimitOverload_LongPosition_EmitsSellLimitFullQty` | `(null, 2, 100.0)` | `(null, 2, 100.0, 100.0)` |
| 2 | `Flatten_LimitOverload_ShortPosition_EmitsBuyToCoverLimitFullQty` | `(null, 3, 4800.0)` | `(null, 3, 4800.0, 4800.0)` |
| 3 | `Trim_LimitOverload_LongPosition_EmitsSellLimitAtRefPlusTick` | `(null, 2, 100.0)` | `(null, 2, 100.0, 100.0)` |
| 4 | `Trim_LimitOverload_ShortPosition_EmitsBuyToCoverLimitAtRefMinusTick` | `(null, 2, 100.0)` | `(null, 2, 100.0, 100.0)` |
| 5 | `Flatten_ZeroBuffer_FallsBackToMarketOrder` | `(null, 0, 100.0)` and `(null, 2, 0.0)` | `(null, 0, 100.0, 100.0)` and `(null, 2, 0.0, 0.0)` |

For each test, update the `typeof(...)` type array from `{Instrument, int, double}` to `{Instrument, int, double, double}` and update the reflection invocation parameter arrays to match.

---

### CopyEngineTests.cs — ADD 5 new [Fact] tests

```csharp
[Fact]
public void TrimLimit_Long_PlacesAboveAsk()
{
    double px = CopyEngine.ComputeLimitPx(isLong: true, ask: 5000.25, bid: 5000.00, exitBuffer: 1, tickSize: 0.25);
    Assert.Equal(5000.50, px, precision: 10);
}

[Fact]
public void TrimLimit_Short_PlacesBelowBid()
{
    double px = CopyEngine.ComputeLimitPx(isLong: false, ask: 5000.25, bid: 5000.00, exitBuffer: 1, tickSize: 0.25);
    Assert.Equal(4999.75, px, precision: 10);
}

[Fact]
public void FlattenLimit_Long_PlacesAboveAsk()
{
    double px = CopyEngine.ComputeLimitPx(isLong: true, ask: 5000.25, bid: 0, exitBuffer: 2, tickSize: 0.25);
    Assert.Equal(5000.75, px, precision: 10);
}

[Fact]
public void FlattenLimit_Short_PlacesBelowBid()
{
    double px = CopyEngine.ComputeLimitPx(isLong: false, ask: 0, bid: 5000.00, exitBuffer: 2, tickSize: 0.25);
    Assert.Equal(4999.50, px, precision: 10);
}

[Fact]
public void TrimLimit_FallsBackToMarket_WhenAskIsZero()
{
    var ex1 = Record.Exception(() => _engine.Trim(null, 2, 0.0, 99.75));
    Assert.Null(ex1);
    var ex2 = Record.Exception(() => _engine.Trim(null, 2, 100.25, 0.0));
    Assert.Null(ex2);
    var ex3 = Record.Exception(() => _engine.Trim(null, 0, 100.25, 99.75));
    Assert.Null(ex3);
}
```

---

### CYC table

| Method | File | CYC |
|--------|------|-----|
| `ComputeLimitPx` | CopyEngine.cs | 1 |
| `GetAsk()` | TradeCopierPanel.cs | 4 |
| `GetBid()` | TradeCopierPanel.cs | 4 |
| `OnTrimClick` | TradeCopierPanel.cs | 4 |
| `OnFlattenClick` | TradeCopierPanel.cs | 4 |
| `Trim(Instrument,int,double,double)` | CopyEngine.cs | 6 |
| `Flatten(Instrument,int,double,double)` | CopyEngine.cs | 6 |

---

### JS rule constraints per method

| Method | Rules |
|--------|-------|
| All methods | JS-021: no lock() |
| All methods | JS-033: no async void (none here) |
| `GetAsk`, `GetBid` | JS-002: no return null — returns 0.0 sentinel instead |
| `ComputeLimitPx` | ASCII-only identifiers |
| `Trim`, `Flatten` 4-arg | JS-001: no throw in business logic — exception caught and routed to StatusUpdate |
| All CreateOrder calls | PTT- prefix mandate: "PTT-TrimLimit" and "PTT-FlattenLimit" |
| All timestamps | DateTime.MaxValue (not DateTime.Now / DateTime.UtcNow) |

---

### 7-SCAN checklist (engineer contract)

The engineer MUST verify ALL 7 scans pass with the stated result before closing this ticket.

| # | Scan | Command | Required result |
|---|------|---------|----------------|
| SCAN-01 | No lock() in scope | `grep -n "lock(" CopyEngine.cs TradeCopierPanel.cs` | 0 results |
| SCAN-02 | No async void | `grep -n "async void " CopyEngine.cs TradeCopierPanel.cs` | 0 results |
| SCAN-03 | No return null | `grep -n "return null" CopyEngine.cs TradeCopierPanel.cs` | 0 results |
| SCAN-04 | No stale 3-arg Trim/Flatten calls | `grep -n "_engine\.Trim\|_engine\.Flatten" TradeCopierPanel.cs` | All occurrences have 4 args (ask, bid) |
| SCAN-05 | GetRefPrice removed | `grep -n "GetRefPrice" TradeCopierPanel.cs` | 0 results |
| SCAN-06 | No bare .Ask/.Bid/.Last without .Price | `grep -n "\.Ask[^.]\\|\.Bid[^.]\\|\.Last[^.]" TradeCopierPanel.cs` | 0 results |
| SCAN-07 | PTT order name coverage | `grep -n "PTT-TrimLimit\|PTT-FlattenLimit" CopyEngine.cs` | Exactly 2 results |
